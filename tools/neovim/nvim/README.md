# Neovim

```
# Go tools
go install golang.org/x/tools/gopls@latest
go install mvdan.cc/gofumpt@latest
go install golang.org/x/tools/cmd/goimports@latest
go install github.com/go-delve/delve/cmd/dlv@latest

# Mason will install gopls automatically if configured but installing global tools is still useful.

# Optional: install neovim nightly / 0.10+ if you don't have it
# macOS (homebrew): brew install --HEAD neovim
```

