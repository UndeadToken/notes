ENV

This instruction is used to define environment variables. using ENV creates a zero-byte-sized additional
layer. ENV persists into running containers ARG does not.
docker container inspect --format 'json .Config' | jq '.Env'
docker container run --rm --env foo="bar" env-demo:1.0 env

ENV <key> <value> ...
ENV foo=bar fizz=buzz
