Events & Messages

- During a transition, messages or events can be sent
- Messages can be sent to other contracts, or accounts
- Emitted events are consumed outside of the blockchain

Send a message:

let one_msg = 
	fun (msg : Message) =>
	let nil_msg = Nil {Message} in
		Cons {Message} msg nil_msg
...
msg1 = { _tag : "setFoo"; _recipient
 : contractAddress1; _amount :
 Uint128 0; foo : Uint32 101 };

msgs = one_msg msg;
send msgs;

Emit an event:

e = { _eventname : "e_name";
<entry>_2 ; <entry>_3};
event e