private key:
openssl genrsa -out private_key.pem 1024

public key:
openssl rsa -in private_key.pem -out public_key.pem -outform PEM -pubout

# gpg
base64 encode: cat file.dat | base64 > file.base64
encrypt: gpg -c file.base64
decrypt: gpg file.base64.gpg
